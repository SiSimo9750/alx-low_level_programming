#include <stdio.h>
/**
 * main - will use printf function to show text
 * Return: will return 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
