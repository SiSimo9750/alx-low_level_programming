#include "main.h"
/**
 * add - will add two integers
 * @number1: first numer
 * @number2: seconde number
 * Return: will return the result
 */
int add(int number1, int number2)
{
	int result;

	result = number1 + number2;
	return (result);
}
